# FastAPI Authentication System (Registration & Login)

This project demonstrates core authentication functionality (registration and login) using **FastAPI**.

## Installation Instructions
1. **Clone the Repository**:
   git clone https://github.com/piyu318318/FastAPiS.git
2. **Make changes in the database.properties**:
3. **Install all required libraries**
  using the command pip install -r requirements.txt
3. **Create tables in the database**
   FastAPiS/refer dbScript.sql
4.**start the server**
   Use Commnad -uvicorn app:app







